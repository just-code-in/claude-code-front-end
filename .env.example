# Matrix Terminal Chatbot Environment Variables
# Copy this file to .env and update with your values

# n8n Webhook Configuration
# Replace with your actual n8n webhook URL
VITE_WEBHOOK_URL=https://your-n8n-instance.com/webhook/your-webhook-path

# Example:
# VITE_WEBHOOK_URL=https://n8n.gracecliff.com/webhook/UI_claude_code

# Note: The VITE_ prefix is required for Vite-based builds
# For static deployment (like this project), the variable will be injected at build time